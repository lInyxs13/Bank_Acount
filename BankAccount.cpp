#include <iostream>
#include "BankAccount.h"

//set the customer name to parameter newName, set the checking account 
//balance to parameter chBalance and set the savings account balance to 
//parameter sBalance.
BankAccount::BankAccount(string newName, double chBalance, double sBalance) {
    newName = hi;
}

//set the customer name to parameter newName
void BankAccount::SetName(string newName) {
    customer = newName;
    dodod = newName;
}
